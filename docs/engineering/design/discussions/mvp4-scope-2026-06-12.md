# MVP-4 scope — 插件就绪：渲染统一 + 主题层 + 契约收紧

| Field | Value |
|---|---|
| Status | in progress |
| Spec | [2026-06-12-mvp4-render-theme-design.md](../../../superpowers/specs/2026-06-12-mvp4-render-theme-design.md)（owner ratified，含钉选形态替换）|
| Branch | feat/mvp4 |

## 决策摘录（详见 spec）

- M4-D1 渲染统一：RenderView + tokens → 共享包；server publish 走 renderToStaticMarkup；publishedHtml = f(publishedDoc, slug, 有效主题) 纯函数
- M4-D2 Theme seam：实例级 settings + 逐页钉选（theme_id 可空 = 跟随实例）；换主题全量重渲染（不可跳过——跳过会产生不可再现状态）
- M4-D3 第三 kind = code（压测：per-block 设置项 / 重渲染依赖 / 三处渲染一致）；不含执行
- M4-D4 契约收紧：CONTRACT.md + 全 kind 仅经 registry；HostServices context = 插件 host API 的雏形
- M4-D5 格式 v2：settings + themeId 进导出物；第一对真实 up/down transform；?format=1 导出端降级实战
- M4-D6 边界：theme 只管内容面（画布/读页/静态页）；chrome 留默认 token（theme-system future）

## Build log

（按时间追加）
1. **packages/theme**：Theme 类型 + graph-paper（现外观固化）+ ink（最小第二主题）+ 参数化 helpers + ThemeContext。坑：context.tsx ↔ index.ts 循环导入在 Bun 下 TDZ 崩（vitest 容忍）——拆 themes.ts 解环。
2. **packages/block-kinds**：git mv 整体迁移 markdown/image + registry + 测试；theme 改经 useTheme()；ImageEditView 的 uploadBlob 改经 HostContext（useHost）——插件 host API 雏形落地。web 侧 tokens.ts 退化为 chrome shim（M4-D6 边界）。
3. **静态渲染统一**：PublishedCanvas（自 ReadPage 标记提升）+ static.ts（renderToStaticMarkup + 文档外壳）；server publish-html.ts 退化为 re-export shim，手写 per-kind 渲染层删除。server 71 测试零修改通过（static-html 测试本就是结构断言）。
4. **Theme 管线**（migration 0006，schema v6）：settings 表 + notepages.theme_id；effectiveTheme（钉选 ?? 实例 ?? 默认，未知降级）；rerenderAllPublished；端点 GET/PUT settings、POST :id/theme（即时重渲染该页）、GET /api/public/instance。
5. **code kind**：highlight.js core + 15 语言子集（sync API，server/browser 同构）；language select = 契约第一个 per-block 设置项；未知语言降级 plaintext 且输出始终转义。Palette 遍历 registry，零改动出现。
6. **Web 接线**：Shell 携带 instanceTheme；EditorPage 画布套 ThemeProvider（有效主题）+ 钉选下拉；ReadPage 改用共享 PublishedCanvas；Sidebar admin 实例主题切换（confirm 提示全量重渲染）。
7. **格式 v2**：FORMAT_VERSION=2；manifest.settings + page.themeId；**第一对真实 up/down transform**（up 补默认无损；down 丢主题字段逐项报损失）；?format=1 + dryRun 导出端降级实战可用。v1 bundle 经真实 up transform 导入验证通过。
8. **文档**：CONTRACT.md（block-kinds 正式契约）+ ADR-0024 + README 行 + runbook 格式 v2 节 + AUDIT 双渲染器债标记 repaid。
9. **全量验证**：server 75 / engine 44 / theme 4 / block-kinds 12 全绿；四处 typecheck 干净。Dockerfile 补 COPY 新包（workspace 解析失败实测抓到）。
10. **容器 v2 round-trip**：ink 实例 + graph-paper 钉选页 + code block 全部穿越 export→import；换主题 rerendered:2；静态页 hljs 高亮在位。
11. **跨版本实测（本轮核心验证）**：mvp-3 tag 经 worktree 构建真实旧镜像。v2 实例 `?format=1` 降级导出（dryRun 损失清单精确两条）→ **旧镜像 import 成功**，页面 200；code block 在旧构建渲染 "Unsupported content"（contained-failure 规则跨版本生效，数据保留）；v2 包直接喂旧镜像 → **409 拒绝**（新格式护栏在真实旧版本验证）。保数据降级路径全链路打通。
12. **Playwright 冒烟**：侧栏实例主题选择器 / 编辑器钉选下拉（instance/📌×2）/ palette Code kind 全部渲染；owner dev 数据未动（dev.db 经 --watch 自动迁到 v6）。
13. **code kind 标记 draft**（owner 反馈）：编辑器缺 tab/括号补全/缩进保持——label 改 "Code (draft)"、EditView placeholder 注明、模块注释 + AUDIT 挂账（编辑器升级 pass 大概率与可执行 code family 同期）。owner 已实测 mvp-3 旧包导入，"都很完美"。
