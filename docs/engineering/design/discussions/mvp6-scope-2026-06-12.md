# MVP-6 scope — Properties 轮：选择驱动的属性检查器 + 作者级外观

| Field | Value |
|---|---|
| Status | in progress |
| Spec | [2026-06-12-mvp6-design.md](../../superpowers/specs/2026-06-12-mvp6-design.md) |
| Branch | feat/mvp6 |
| 前置 | mvp-5（tools 贡献点 / ui-kit / 主题自定义 [ADR-0026]）|

Owner 愿景（2026-06-12 对话，原话要点）：右键背景可改背景颜色/铺图片；右键 block 可改它现在的样子（纸质换白卡、调纹路、隐藏 image 底纸只显示图）；未来 richtext 选中文字时自己调用这个面板声明可改项（字体/颜色/大小/间距）；或者像 3D 引擎侧栏——点一个 item 显示它的 properties。Owner 确认该物通常叫 **properties**。同轮裁定：richtext 不急、主题（operator 级）调整到此为止。

## 决策摘录（详见 spec）

- **M6-D1 模型 = selection → contributors → property sections**：选择类型 page | block |（预留 text-range）；贡献者三类 = host（页面级属性）、theme（外壳策展选项）、kind module（内容工具，MVP-5 已有）。现 ToolPanel 是该模型的退化形态，升级为 Properties 面板。
- **M6-D2 形态先停靠后悬浮**：检查器停靠于侧栏目录下方（owner 的 3D 引擎心智模型），右键悬浮菜单 = 同一属性区块的快捷投影，后做。形态 = dev 决策，内核（属性区块模型）才是产品锁。
- **M6-D3 作者级外观 = 第三自定义轴**，纪律与 M5-D3 同构：**theme 策展块壳选项，作者在选项内挑**（不是自由改壳——否则从后门绕回自由调色）。`Theme.blockShellOptions?` 声明 per-kind 可选外壳变体与开关；块存 `shellChoice`（presentation 数据，不进 kind content）。
- **M6-D4 页面背景**：页面级属性（背景色 / blob 图片引用），同样 theme 可策展可选背景集；进 publish 快照渲染。
- **M6-D5 存储与格式**：blocks/page 级新外观字段 → schema migration + **format v4 成对 transform**（down 丢外观逐项报损失）。
- **M6-D6 还 ADR-0026 挂账**：stationery 槽位组件改"渲染时读参数"（块壳选项的前提），顺带解锁 stationery palettes。
- **M6-D7 richtext 预留**：selection 模型含 text-range 类型占位 + 贡献接口形状；不实现。

## Build log

（按时间追加）
1. **Task 1 registry 解链**（5a38e4c）：THEMES 迁入 registry.ts（唯一 import 具体主题文件的模块）；stationery 槽位组件 closure-over-TOKENS → useTheme() 渲染时读 token——import 环已断，`bun -e` 实测无 TDZ。palettes 对深槽位主题真实生效，stationery 得 kraft 变体（策展纪律：不动 globalCss 硬编码的纸面色族）。
2. **Task 2 壳与背景**（db593d7）：ShellOption 契约 + BlockFrameProps.shell + CanvasSurfaceProps.background。stationery card（净卡纸，胶带保留——胶带是"钉"不是"纸"）/ bare（内容直接落桌——拍立得只剩照片，owner 原话场景）；缺省 frame 通用 flat（token-only 主题零成本得壳选项）。页面背景由 host 在画布根应用（色替换画布、图 cover 垫底），主题 surface 纹理画在其上（透明基底天然合成）。测试坑：断言不能用裸类名（globalCss 里有同名选择器）——用 `class="…"` 形式。
3. **Task 3 server**（b0ef060）：migration 0007（schema v7，dev.db --watch 自动迁移实测）。**两态纠偏**：背景是工作态、快照带副本，公开页只在显式 publish 时变——初版在背景端点里的"即时重渲染"是无效操作（快照没变），已删；与钉选（render-time 元数据）的语义差异记入 ADR。blob-refs 枚举背景 hash（GC 实测保活）。
4. **Task 4 Properties**（caba395）：Selection = page | block，activeId 退化为派生；检查器经 createPortal 注入 Sidebar 锚点（保编辑器 theme/host context；MutationObserver 跟踪侧栏折叠重建）。悬浮 ToolPanel 退役并入。冒烟全链路：背景色即改即见（工作态预览）→ 壳 flat 自动保存 → publish → 静态页 `data-shell="flat"` + 背景色。**发现 owner 在轮间自己玩过 Theme studio**（workbench+forest+手写体、blueprint sepia、改钉选/重力）——dev 库状态是 owner 的，测试改用专建页并删除。
5. **Task 5 format v4**（414727c）：up 补 null 保 canonical 键序；down 从工作态**和已发布快照**双双剥离外观、逐项报损失；importer 校验形状 + 背景 blob 必须随包到达（422 实测）。server 86 测试全绿。
