# MVP-9 范围讨论 — richtext kind 轮（2026-06-13）

Status: in progress（owner 开盘裁定 2026-06-13）

## 背景

MVP-8（通用面板轮）merge 后 owner 裁定开 MVP-9。挂账候选：richtext kind /
页间一等链接 / 产品内搜索 / chrome 槽位契约。我方推荐 richtext 主线（dogfooding
摩擦反复印证 + 浮层原语刚就位），并列出三个待裁问题（引擎选型 / 与 markdown
关系 / 页间链接是否搭车）。

## Owner 裁定

### M9-D1 · 开盘三裁（2026-06-13）

> "proseMirror，并存，页间链接可以搭车。选重的原因是后面这个richtext block可以
> 独立成插件，不是所有人都需要的。而且有一个如此复杂的proseMirror我们也可以
> 压力测试一次plugin system。"

1. **引擎 = ProseMirror**。选重不是为了功能堆叠，而是战略性的：
   richtext block 未来要**独立成插件**（不是所有人都需要），一个足够复杂的
   依赖恰好可以**压力测试 plugin system**——契约哪里漏、HostServices 哪里缺，
   这轮全部记录在案。
2. **与 markdown 并存**：两种 kind 各活各的，不替代不强转。
3. **页间一等链接搭车**：链接 mark 存 pageId（改名不断链），随 richtext 落地。

## 设计桩

**插件姿势纪律（压力测试的测法）**：richtext 整个住
`packages/block-kinds/src/richtext/`，只许消费 plugin 能拿到的面——
registry 注册、HostServices、ThemeContext、ui-kit。**禁止** import apps/web
的任何东西（chrome/overlays 不可达——这是故意的：插件够不着 chrome）。
凡是它需要而契约没有的能力，就是 plugin system 的一个 finding，
加进 HostServices（可选字段，旧 host 不破）并记录。

**服务端导入安全**：publish 管线（static.ts → registry → 各 kind）在 Bun
无 DOM 环境跑。RenderView = 纯 PM-JSON → React 树行走器，**零 prosemirror
依赖**；prosemirror-view 只在 EditView 的 effect 里动态 import。

**内容格式**：`{ doc: <ProseMirror JSON> }`，自定义小 schema（与渲染行走器
锁步）：paragraph / heading 1-3 / bullet·ordered list / blockquote /
hard_break + marks strong / em / code / link(href) / **pagelink(pageId)**。

**页间一等链接**：pagelink mark 只存 pageId（身份），不存 slug（地址）；
渲染为 `/p/:pageId` 永链；服务端新路由 `GET /p/:id` → 已发布公开页 302 到
当前 `/notes/:slug`，否则 404（与 slug 路由同款 no-leak）。
当前 slug 政策下（首发锁定、永不变 [publish 端点注释]）"一等"的实际收益：
① 可以链接**尚未首发**的页面——首发前 slug 还是建页占位，存 slug 必断，
存 pageId 则目标首发后 302 自动接通；② 对未来任何 slug 政策变化免疫。
显示文本不自动跟改名（backlink/自动标题 = 后续轮）。

## Owner 裁定（续）

### M9-D2 · 编辑面 Notion 化（2026-06-13）

> "rich block做的太过于简单了，block上面的选择栏做到悬浮窗口里，可以整体上参考notion."

撤掉块顶固定工具条；格式化动作进悬浮窗口，整体参照 Notion。

### M9-D3 · 菜单统一面 + 颜色/间距（2026-06-13）

> "竖栏更好，跟右键菜单统一最好。使用同一个通用菜单面。以及字体颜色间距的功能
> 最好也要有"

横向气泡退役；选区格式菜单与 slash 菜单改为竖栏，**与右键菜单同一张通用菜单面**；
新增字体颜色与行距功能。

## Build log

（按时间追加）

1. **2026-06-13 · richtext kind 首刀（M9-D1 全三裁落地）** —
   `packages/block-kinds/src/richtext/`：richtext.ts（内容模型 + 零 PM 的 JSON
   行走器：extractText/coerceContent/linkedPageIds）、schema.ts（自定义小 schema，
   与行走器锁步：p/h1-3/列表/引用/hard_break + strong/em/code/link/**pagelink**）、
   RichtextRenderView（纯 JSON→React，发布管线 Bun 无 DOM 可跑；href 消毒
   javascript:→#；未知节点降级保内容）、editor.ts（PM view/state/commands/
   history/inputrules 全在此，**只经 EditView 动态 import**——发布管线与读者
   bundle 永不加载）、RichtextEditView（模块自带工具条 B/I/code/H1-3/列表/引用/
   link/⛓page，mousedown preventDefault 保选区；`## `/`- `/`1. `/`> ` inputrules；
   Mod-B/I/\` keymap）。registry 注册第 4 kind（与 markdown 并存）。
   **Plugin 压力测试 findings（HostServices 新增可选能力，旧 host 不破）**：
   ① `listPages`——插件够不着 host 树状态，链接 picker 需要页面目录；
   ② `promptText`——插件不得自有 window.* 对话框，chrome overlays 又是 host
   私有，必须由 host 出借模态。apps/web 在 EditorPage 接线（listPages 滤掉当前页，
   promptText → overlays.prompt）。
   **页间一等链接**：pagelink 存 pageId，渲染 `/p/:id`；服务端 publicHtmlRoutes
   新增 `GET /p/:id`（公开已发布 → 302 当前 slug；否则 no-leak 404）；
   index.ts 转发 + vite `^/p/.+` 代理。实测：未首发 404 → 首发后 302 接通
   （"链接未首发页"场景成立）；slug 首发锁定的既有政策下永链语义如 scope 注记。
   测试：block-kinds 新增 richtext 11 条（行走器/锁步/消毒/降级，renderToStaticMarkup
   = 发布管线同款渲染模式），全仓 120 测绿。Playwright 走查：右键插入 richtext →
   PM 挂载聚焦 → `## ` inputrule → 工具条加粗 → ⛓page picker 过滤选页 →
   RenderView/静态 HTML 均含 `/p/:id` + `<strong>` → curl 302 Location 正确。

2. **2026-06-13 · M9-D2 编辑面 Notion 化** —
   固定工具条退役。三件浮具（全部模块自有 portal，插件姿势不破）：
   ① **气泡栏**——选区非空 + PM 持焦时浮在选区正上方居中（B/I/code、H1-3、
   列表、引用、link、⛓page），mousedown preventDefault 保选区；
   ② **slash 菜单**——editor.ts `handleTextInput` 捕获 "/"（不消费，字符照打），
   浮在光标下：H1-3/列表/引用/Link to page，↑↓ 高亮、Enter 选中（window
   capture 监听，PM 看不见导航键）、Esc 关、继续打字自动关；选中后删掉 "/"
   再执行；③ **页链 picker** 浮窗化，气泡 ⛓（选区加 mark）与 slash（光标插
   标题文本）双入口共用。
   配套：EditorHandle 新增 hasFocus/selectionCoords/posCoords/deleteRange；
   浮板跟随 scroll/resize/focusin 重定位（viewport 坐标 + 测量后钳位，
   上方放不下翻到下方）；空文档 CSS `:has` 占位提示（"Write — or '/' for
   blocks…"）。z 600 < host 模态/菜单 1000+。
   Playwright 实走：占位符渲染 → 选词气泡浮现（11 按钮）→ 气泡加粗 →
   "/" 菜单 7 项 → 6×↓+Enter 开 picker 且 "/" 已删 → 选页插入 pagelink。
   全仓 120 测绿。

3. **2026-06-13 · M9-D3 菜单统一面 + 颜色/间距** —
   **Plugin finding #3 = `HostServices.menu`**：通用菜单面是 host 的视觉主权，
   插件描述 items（item/separator/label/choices，types.ts 自有 HostMenuItem
   镜像 chrome 形状不 import），host 负责画。chrome MenuItem 的 item 增
   `checked`（✓ 前缀 + menuitemcheckbox role），所有 context menu 共享受益。
   选区格式菜单（拖选 mouseup / 选区上右键重入，editor.ts handleDOMEvents；
   空选区右键保留原生菜单）：Bold/Italic/Inline code（✓ 态）+ turn into 药丸行
   + **color 九色点**（固定调色板 COLOR_PALETTE 存纯 CSS 色值进 doc——换主题
   不漂移；RenderView 画前再过 isSafeCssColor，import 来的 doc 不可信）+
   **spacing 药丸**（compact/normal/relaxed = 1.3/1.55/1.85，content 级字段
   `spacing`，coerce 白名单校验）+ Link…/Link to page…。slash 菜单同样改走
   host.menu（键盘导航白得，MenuPanel 自带）。模块仅存的自有浮层 = 页链
   picker（需要输入框，菜单面刻意不承载）。横向气泡与 FloatingPanel 选区跟随
   机制退役。
   测试：color 消毒（恶意 style 不落 DOM）+ spacing 行高/coerce 白名单，
   block-kinds 40 条全绿。Playwright：选词 mouseup → 竖栏 TEXT 菜单（通用面）
   → Red 色点上色 `<span style="color: oklch(...)">` → 选区右键重入 →
   relaxed 行距即时 1.85 → autosave 后 spacing/color 均落库。
