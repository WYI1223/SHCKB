# MVP-6 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Selection→Properties 检查器 + theme 策展块壳选项 + 页面背景 + format v4（spec: [2026-06-12-mvp6-design.md](../specs/2026-06-12-mvp6-design.md)）。

**Architecture:** 依赖顺序 1→2→3 不可调换（registry 重构是壳选项前提；壳选项与背景是 Properties 面板的内容；format v4 收尾）。

---

### Task 1: theme 包 registry 重构（TDZ 解链）

**Files:** Modify `packages/theme/src/{themes.ts,context.tsx,workbench.ts,stationery.tsx,blueprint.ts,index.ts}`; Create `packages/theme/src/registry.ts`

- [ ] THEMES/DEFAULT_THEME_ID 迁入 registry.ts；themes.ts 移除主题文件 import
- [ ] stationery 槽位组件改 useTheme() 渲染时读 token（TOKENS 仅构造用）；workbench/blueprint 同步检查
- [ ] stationery 增 `kraft` palette 变体（牛皮纸调，颜色族 token only）
- [ ] `bun run` 实测无 TDZ（不是只跑 vitest）；theme 测试全绿；vite 重启
- [ ] Commit

### Task 2: 块壳选项 + 页面背景类型

**Files:** Modify `packages/theme/src/themes.ts`（ShellOption / Theme.shellOptions / BlockFrameProps.shell / CanvasSurfaceProps.background + PageBackground 类型）、`stationery.tsx`（paper/card/bare 三壳 + surface 接背景）、`workbench.ts`（outlined/flat）、`packages/block-kinds/src/{frames.tsx,PublishedCanvas.tsx}`（透传 shell/background）; Test theme + block-kinds

- [ ] 类型 + stationery 三壳实现（card = 白卡无撕边无胶带；bare = 无底纸直出内容）+ workbench flat
- [ ] 缺省 surface/PublishedCanvas：background.color 替换 canvasBg、blobHash cover 垫底
- [ ] 测试：未知 shell 降级默认壳；静态渲染含 shell 分支与背景层
- [ ] Commit

### Task 3: server — schema v7 + 背景端点 + 快照外观

**Files:** apps/server migration 0007（blocks.shell, notepages.background）、schema.ts、routes/notepages.ts（working-state 接受 shell；POST :id/background 校验+即时重渲染；publish 快照含 shell/background）、render/publish-html 透传; Test server

- [ ] Migration + 校验（shell string|null；background JSON，blobHash 必须在 blobs 表）
- [ ] publish/读路由/静态渲染全链路携带外观；blob-refs 枚举含 background.blobHash（GC/export 契约）
- [ ] Commit

### Task 4: web — Selection + Properties 面板

**Files:** Create `apps/web/src/grid/Properties.tsx`; Modify `EditorPage.tsx`（selection state、portal 注入、working-state 带 shell）、`Sidebar.tsx`（底部锚点 div）、`GridCanvas.tsx`（点空白 = page selection）、api/client.ts（setPageBackground）; Delete `ToolPanel.tsx`（内容并入）

- [ ] Selection 类型 + 派生 activeId；page 区块（背景色 input + 图片上传/清除）；block 区块（壳选项 chips 仅当主题策展 + 既有 module tools）
- [ ] Playwright 冒烟：页面属性/背景即改即见/壳选项 bare 隐藏底纸/发布同步
- [ ] Commit

### Task 5: format v4

**Files:** apps/server/src/export/{format.ts,exporter.ts,importer.ts,migrate-format.ts}; Test export 套件

- [ ] FORMAT_VERSION=4；up 补 null 无损；down 丢 background/shell 逐项报损失；importer 校验
- [ ] v3 包 up 导入 + v4 round-trip + ?format=3 dryRun 测试
- [ ] Commit

### Task 6: 收尾

- [ ] ADR-0027（selection/properties 模型 + 作者级外观轴）+ CONTRACT/runbook 增补 + build log + 全量验证 + Docker build
- [ ] Commit
