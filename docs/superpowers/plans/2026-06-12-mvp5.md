# MVP-5 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 场景实证（3 案例 + examples/ 包）→ 通用工具面板（ui-kit 原语 + tools 贡献点）→ 主题自定义层（调色板变体 + 白名单覆写 + format v3）。

**Architecture:** spec [2026-06-12-mvp5-design.md](../specs/2026-06-12-mvp5-design.md)。顺序 0→1→2 不可调换（0 喂需求，1 的原语被 2 复用）。

**Tech Stack:** 既有栈；新增 workspace 包 @skb/ui-kit（React + @skb/theme tokens）。

---

## Phase 0 — 场景案例

### Task 0.1: seed 脚本

**Files:** Create `scripts/seed-examples.ts`（bun 运行；参数 `--base <url> --email <e> --password <p>`）

- [ ] 内置确定性 PNG 生成（纯 bun：手写最小 PNG encoder 或预生成的 base64 内嵌渐变图 6-8 张，≤50KB/张）
- [ ] 登录（better-auth cookie）→ 建文件夹 `示例` → 三案例各建页：日记《六月手帐》(pin stationery)、旅行《海岸线三日》(pin stationery, 图块走 blob 上传)、代码解析《grid-engine 重力算法讲解》(pin workbench, code+markdown 交替)
- [ ] 全部 publish；幂等护栏：文件夹已存在则中止（防重复跑污染）
- [ ] 对 scratch 实例跑通（临时 SHCKB_DB_PATH + 随机端口），人工 Playwright 截图复核三页观感
- [ ] 对 owner dev 库（:3210）跑：**仅** `示例` 文件夹内容
- [ ] Commit

### Task 0.2: examples/ 导出包

- [ ] scratch 实例 `/api/admin/export` → `examples/diary-stationery.zip` 等 3 包（每案例独立 scratch 库导出，包内只含该案例）+ `examples/README.md`（导入指令 + 截图引用）
- [ ] 摩擦清单记入 scope build log（Phase 1/2 需求输入）
- [ ] Commit

## Phase 1 — ui-kit + 工具面板

### Task 1.1: packages/ui-kit

**Files:** Create `packages/ui-kit/{package.json,tsconfig.json,src/index.ts,src/primitives.tsx,src/__tests__/primitives.test.tsx}`; Modify root workspace 配置、`Dockerfile`（COPY ⚠️ mvp-4 坑）

- [ ] UiSelect / UiButton / UiTextInput / UiToggle（token 驱动：accent/hairline/surfaceInsetBg/textColor/mutedColor/blockRadius；不建 context，纯 useTheme 消费——TDZ 不可能成环）
- [ ] 测试：默认主题下渲染 + token 接线断言（borderColor 含 hairline 等）
- [ ] Dockerfile COPY + `bun run --filter '*' test` 全绿 + Commit

### Task 1.2: tools 贡献点

**Files:** Modify `packages/block-kinds/src/types.ts`（BlockToolProps + tools 字段）、`code/index.ts(x)`（语言选择迁出为 tool，EditView 剩 textarea）、`image/`（alt 工具）、`CONTRACT.md`（贡献点章：palette 条目 / tools / 分权规则）; Create `apps/web/src/grid/ToolPanel.tsx`; Modify `EditorPage.tsx`（挂面板，传激活块 content/onChange）; Test `packages/block-kinds/src/__tests__/tools.test.tsx`

- [ ] 类型扩展 + code 语言工具迁移（用 UiSelect）+ image alt 工具（用 UiTextInput；RenderView img alt 接通）
- [ ] ToolPanel：无激活块/无 tools → null；有则按 module.tools 渲染
- [ ] 测试：tools 声明存在、工具 onChange 往返、RenderView 不含工具控件
- [ ] Playwright 冒烟：激活 code 块 → 面板出现 → 切语言生效
- [ ] Commit

## Phase 2 — 主题自定义层

### Task 2.1: theme 类型 + applyCustomization

**Files:** Modify `packages/theme/src/themes.ts`（PaletteVariant / ThemeCustomization / palettes / customizableTokens / applyCustomization 纯函数）、各主题文件（workbench 2 变体、stationery 2 变体、blueprint 1 变体、graph-paper+ink 白名单 fontFamily/accent）; Test `theme.test.ts` 扩展

- [ ] applyCustomization：base → variant tokens → 白名单过滤 overrides；未知 paletteId 忽略；slots 原样
- [ ] 变体只动颜色族 token（slot/pad/blockRadius 禁入变体——测试断言）
- [ ] Commit

### Task 2.2: server 存储/解析/端点

**Files:** Modify `apps/server/src/settings.ts`（themeCustomization 读写 + effectiveTheme 合成）、`routes/admin.ts`（GET/PUT theme-customization：白名单过滤、未知 themeId/paletteId 422、成功后 rerenderAllPublished）; Test `apps/server/test/theme.test.ts` 扩展

- [ ] effectiveTheme 全调用点行为复核（publish / pin / instance 切换 / import 重渲染）
- [ ] Commit

### Task 2.3: web 自定义 UI

**Files:** Modify `apps/web/src/shell/Sidebar.tsx`（主题区扩展）; Create `packages/ui-kit/src/UiPaletteSwatches.tsx`

- [ ] 变体色板行（色点组 + 选中 accent 描边）+ 白名单控件（fontFamily UiSelect / 颜色 color input）→ PUT + confirm 重渲染提示
- [ ] Shell chrome 跟随自定义后的 instance theme
- [ ] Playwright：切变体 → 画布变色 → 静态页同步
- [ ] Commit

### Task 2.4: format v3

**Files:** Modify `apps/server/src/export/{format.ts,exporter.ts,importer.ts,migrate-format.ts}`; Test export 套件扩展

- [ ] FORMAT_VERSION=3；manifest.settings.themeCustomization 可选；up(v2→v3) 无损补缺省；down(v3→v2) 丢字段逐项报损失；importer 白名单再过滤 + 未知 themeId 容忍
- [ ] 测试：v3 round-trip / v2 包 up 导入 / ?format=2 降级 dryRun 损失清单 / 手编越权包被过滤
- [ ] Commit

### Task 2.5: 收尾

- [ ] ADR（theme customization model + tools 贡献点——单 ADR 或两个，按内容密度定）+ CONTRACT.md 终稿 + runbook format v3 节 + AUDIT 更新
- [ ] 全量验证：全包测试 + typecheck + Docker build + Playwright 冒烟；build log 收尾
- [ ] Commit

## Self-review 备注

- 0.1 PNG 生成是唯一无既有先例的步骤——若 bun 手写 PNG 太费，降级方案：`Bun.write` 预生成 base64 内嵌（脚本里数据块）即可，不引依赖
- 2.2 的 422 校验与 2.4 importer 过滤共用同一 sanitize 函数（单一真理源）
- 每 phase 结束跑全量测试，不留红过夜
